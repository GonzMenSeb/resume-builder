Deeply understand the research results we have in this repo and, with them, together with python's claude sdk, let's create a proffessional python project to generate S+ tier resumes from a set of raw data about a person (the one applying for jobs) that can be given as a set of pdfs, raw text, markdown files, etc. Also, you should be able to Optionally specify the job position you are applying to as well, to further optimize the end result to it. Adhere with the best coding practices and keep everything fast and optimal. Also, make sure to run a colorful and beautiful progress-tracking interface in the terminal while you run the pipeline on some specific data. Use your claude skills to achieve better results in all of this process. Do NOT limit yourself to using the templates that already exist in this codebase NOR adhering to the sample resumes... Ignore them if you can.